# Mago PHP toolchain configuration
# Docs: https://mago.carthage.software

php-version = "8.4"

[source]
paths = ["app", "config", "database", "routes", "tests"]
excludes = ["vendor", "node_modules", "storage", "public"]
