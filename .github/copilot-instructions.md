# Copilot instructions

All project conventions for AI agents live in the repository root `AGENTS.md`, which is generated by Laravel Boost
from `.ai/guidelines/`. Follow it. Edit the sources in `.ai/guidelines/` and run `php artisan boost:update`; never edit
`AGENTS.md` by hand.
